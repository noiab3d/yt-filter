// Seletores DOM do YouTube centralizados aqui — ver skill dom-scraping-resilience.
// Nenhum outro ficheiro deve ter seletores hardcoded.

// TODO: confirmar seletor com HTML real da homepage do YouTube (ainda não recebido).
// Palpite razoável baseado em conhecimento geral da estrutura do YouTube (custom element,
// não classe CSS ofuscada), mas não foi verificado contra o DOM atual.
export const VIDEO_GRID_CONTAINER = 'ytd-rich-grid-renderer';
