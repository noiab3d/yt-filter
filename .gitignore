node_modules/
dist/
web-ext-artifacts/
*.log

CLAUDE.md
.claude/

# Dumps de HTML da página inteira usados como referência local (podem conter
# recomendações/dados pessoais) — fixtures pequenos e intencionais vão em tests/fixtures/.
/*.html

# Perfil local do Firefox usado por "npm run dev:firefox" (login, cookies, histórico).
/yt-filter-dev/

# selector-check.js é uma ferramenta só do David, para detetar quando o
# YouTube muda o DOM e reportar aqui para corrigirmos juntos — não faz
# sentido ir para o repo público. O perfil de login (.yt-session/) já era
# gitignored por ser mais sensível ainda.
/tests/selector-check.js
/tests/.yt-session/

# Notas de trabalho pessoais do David, não são documentação do projeto.
/fixes.txt
/AMO-LISTING-DRAFT.md
